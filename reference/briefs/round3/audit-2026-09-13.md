# Audit — 2026-09-13

Method: `tsc` + `eslint` clean, local production build (`next build` → `next start -p 3005`), headless Chrome with a real GPU, desktop 1440×900 and iPhone 14 390×844 dpr3, every route plus SPA navigation between pages and demos, three code-review passes. Console clean on every route. Baseline commit for round 3: `8896be7`.

Severity: 🔴 bug · 🟠 speed · 🟡 inconsistency · ⚪ nit. References are `file:line` at `8896be7`.

Each round-3 brief cites items by their id below (e.g. **B1**, **S4**, **I12**). Work packages: **A** chrome/motion core · **B** site pages & canvas · **C** copy/SEO/config/docs · **D** hands + darkroom · **E** tarassaco + vortex · **F** wall.

---

## B — Bugs (desktop / mobile)

### Verified in the browser

- **B1** 🔴 (A) **Scroll locked after every demo.** Leaving a demo via the exit link or Esc, `html.loading` stays forever and `overflow:hidden` on html+body propagates to every following page until a full reload. Wheel still works only because Lenis scrolls programmatically; scrollbar, keyboard and native touch scroll on phones are blocked. Cause: on the (site) layout remount the layout-effect calls `reveal()` (removes the class) before the `useEffect` adds it again, and the safety timeout never removes it because `doneRef` is already true. `src/components/chrome/Loader.tsx:110-118` vs `:208-224`.
- **B2** 🔴 (C) **Default Next 404**: "404 This page could not be found.", no topbar, footer, style or link back. No `not-found.tsx`.
- **B3** 🔴 (C) **OG shared everywhere**: child pages override only `title`/`description`, so `og:title`, `og:description`, `og:url` stay the home's on every route. Sharing a demo shows the home card. `src/app/layout.tsx:61-73`.
- **B4** 🔴 (C) **`immutable` cache on files overwritten in place**: `cover.webp` of darkroom/hands/wall, the 54 vortex images and the MediaPipe wasm carry `max-age=31536000, immutable` but are not content-addressed. Git shows the covers rewritten on 2026-09-11 (`274ca16`, `20c96a5`); returning visitors keep the old one for a year, and `/_next/image` inherits the TTL. A cached old wasm + new bundle breaks Tarassaco and Mani. `next.config.ts:43-73`. `DECISIONS.md:525-527` "content-addressed by filename" is false.
- **B5** 🔴 (C) **"P.IVA — placeholder"** live in the footer of every page, EN and IT. `src/lib/dictionary.ts:362`, `:574`.

### From the code (verified on source)

- **B6** 🔴 (B) Site canvas has no `webglcontextlost/restored` handler (the demos do; R3F does not): after a context loss (iOS under memory pressure) `canvas-live`, `h1.is-melting` (opacity 0) and `figure.is-live` (img opacity 0) stay → hero name and /about photos disappear. `src/components/canvas/Field.tsx:55-82`, `NameMeltView.tsx:175`, `FigureView.tsx:146`.
- **B7** 🔴 (D) Mani: Escape without `e.repeat`/modifier guard — held half a second it closes the print and then exits the demo. `hands/hooks/useKeyboardHands.ts:46-52`; same at the gate `hands/App.tsx:158-160`.
- **B8** 🔴 (E) Vortex: keys without `altKey/ctrlKey/metaKey/repeat` guard — Alt+← (browser back) also steps the demo; Esc/Backspace held walks gallery → carousel → idle → exit. `src/components/vortex/VortexExperience.jsx:87-110`.
- **B9** 🔴 (E) Tarassaco: the 9 s timer is armed only after `getUserMedia` resolves — with the prompt left open (Firefox/Safari) the gate shows INITIALIZING forever. `tarassaco/hooks/useWindPhysics.ts:245-263,328-333`. `preventDefault()` on every Space keydown even with a `<button>` focused: the error dialog's "continue with the keyboard" does not activate with Space (`:140-144`). No `facingMode: "user"` (`:253-256`). `FaceLandmarker` GPU-only, no CPU fallback (`:201-219`). Root is `position: relative; min-height: 100dvh`, not `fixed; inset: 0`: on a phone poem + 450 px dandelion exceed the viewport, the page scrolls and the flower covers the text (`tarassaco/App.tsx:120`, `tarassaco.css:19-29`, `components/DandelionScene.tsx:27-30`).
- **B10** 🔴 (A) Loader: two timelines run on the same veil on quick navigations (`useGSAP` with `dependencies` and no `revertOnUpdate` does not revert on deps change — `node_modules/@gsap/react/src/index.js:34-45`), veil flickers, tweens pile up. `Loader.tsx:166-203`.
- **B11** 🔴 (A) Loader veil lock is only `overflow:hidden`: Lenis is not stopped and `data-lenis-prevent` is missing (home/about set it) — wheel during the veil = programmatic `scrollTo` that `overflow:hidden` does not block → /websites, /xperiments, /graphic-designs reveal already scrolled. `Loader.tsx:208-211` + `globals.css:201-204`.
- **B12** 🔴 (A) Topbar entrance (1.15 s hidden + 1.2 s fade, `html.entering`) has no `veilPlayed` equivalent: returning from a demo the topbar disappears for 1.15 s and the field bloom repeats while the veil does the fast dissolve. `src/components/chrome/Shell.tsx:85-90`.
- **B13** 🔴 (A) `reducedMotion` starts `false` and settles in an effect: every consumer does a pass in motion mode — Loader timeline starts then the veil unmounts (tweens on detached nodes, `enter()` called), HomeSequence applies lock + `home-live` + `scrollTo(0,0)`, Lenis is created and recreated, Cursor attaches/detaches. `src/components/providers/AppProvider.tsx:55,61-70`. A synchronous snapshot hook already exists: `src/lib/use-reduced-motion.ts`; `AboutSequence.tsx:74-76` re-reads the MQ to work around this. `globals.css:832-841` `.teaser{opacity:0}` is not inside `no-preference` → flash.
- **B14** 🔴 (C) `viewport` without `viewportFit: "cover"`: every `env(safe-area-inset-bottom)` is 0 on iOS, footer under the home indicator. `src/app/layout.tsx:78-81`.
- **B15** 🔴 (D) Mani shader ignores texture alpha (`gl_FragColor = vec4(col, u_opacity * cut)`); 6 of 14 prints have an alpha channel (img_001, 011, 021, 029, 044, 050) and show stored RGB in transparent zones. `hands/engine/cardMaterial.ts:75-89`. Darkroom multiplies by `print.a` on the same files.
- **B16** 🔴 (B) Row reveal height tweened to fixed px (`inner.offsetHeight`) while `.reveal-inner` is `clamp(220px,36vh,400px)`: resize with a row open leaves a gap or clipping. `src/components/work/Row.tsx:90-98` + `globals.css:1163`.
- **B17** 🔴 (B) /about keyboard: arrows move the track but not focus; with focus on a panel-2 link, ArrowDown goes to 3 and the next Tab (`onFocusIn` :266-273) drags the track back. `src/components/about/AboutSequence.tsx:236-273`.
- **B18** 🔴 (A) `scrollbar-gutter: stable` stays active under `overflow:hidden` (home/about): on Windows with classic scrollbars a ~15 px black strip on the right outside the fixed canvas and the topbar scrim — the artefact fixed only for `.immersive` (`globals.css:41-48`). `globals.css:39`.
- **B19** 🔴 (A) Loader: "lifts once the real page has streamed in" is false — the sweep is a fixed 0.82 s; on a slow network the veil lifts on the empty `(site)/loading.tsx:14` fallback (60vh blank). `Loader.tsx:159-161`.
- **B20** 🟡 (E) Vortex `.vortex-frame` uses `100vh`: on iOS with the bar the centre shifts (`globals.css:2381-2382`). WebGL probe accepts WebGL1 but three r186 is WebGL2-only: a WebGL1 browser hits the generic `error.tsx` instead of `copy.noWebgl`; probe contexts never released. `src/components/vortex/components/VortexScene.jsx:32-40` (others use the shared `hasWebGL2()`).
- **B21** 🟡 (D) Darkroom `.darkroom-next` is ~28 px tall: the only control of the brush path (reduced motion) on touch. `darkroom/darkroom.css:44-71`.
- **B22** 🟡 (D) Mani caption at `top = min(0.86, bottom)` + `margin-top: 1.5rem`: on a landscape phone (~360 px tall) ~26 px remain for two lines, "swipe to close" gets cut. `hands/App.tsx:190` + `hands.css:99-115`.
- **B23** 🟡 (F) Wall: "pitch" station pulse starts from `setTimeout(flightSeconds)`, not from the rig's `settle`: with slow frames (dt clamped 0.1 s) the pulse starts before landing. `wall/components/WallScene.tsx:123-133`. `CameraRig.tsx:266-278` `first.current` guard consumed by StrictMode double effect (dev only).
- **B24** ⚪ (D/E/F) `webglcontextlost/restored` listeners never removed: `darkroom/components/DarkroomCanvas.tsx:32-36`, `hands/components/HandsScene.tsx:56-57`, `wall/components/WallCanvas.tsx:67-71`. `hands/hooks/useHandTracker.ts:190-206,66` console stays patched if the wasm fetch never resolves. `hands/engine/world.ts:206-219` `loader.load` without error callback (a failed print never appears, silently). `tarassaco/components/IntroScene.tsx:32-41` 50 ms `setTimeout` not cleared on unmount. `VortexLayout.jsx:182-191`, `VortexCard.jsx:166-177` hover/highlight tweens not killed on unmount.
- **B25** ⚪ (B) `src/components/chrome/Cursor.tsx:35-41` does not filter `pointerType === "touch"` (dot jumps under the finger on touch laptops) and the cursor never hides when the pointer leaves the window; `:60` `.g-enter` exists nowhere. (Cursor is chrome → **A**.)
- **B26** ⚪ (B) `HomeSequence.tsx:209,227` and `AboutSequence.tsx:103,333` restore `history.scrollRestoration` to `"auto"` instead of the previous value. `HomeSequence.tsx:234-268,339-346`: a resize across 860 px after composition re-locks, `scrollTo(0,0)`, unlocks and slides to the footer after 1.2 s.

## S — Speed

- **S1** 🟠 (B) **three + fiber + drei (≈1.2 MB raw; ≈430 KB gz total JS) on every site route**, /websites and /xperiments included, on phones too, for the ambient background field. No gate on `saveData`, `deviceMemory`, `hardwareConcurrency`. Chunk `3w8_…js` 882 KB raw.
- **S2** 🟠 (E) **Vortex**: 54 images (1.7 MB) fetched eagerly on entry, 650 ms long task on desktop and mobile. `useLoader(TextureLoader)` cache never cleared (`useLoader.clear`): decoded `<img>` + `Texture` (≈60–80 MB RAM) stay for the session after exit. `VortexCard.jsx:60`. `GalleryScene.jsx:183-187` `new THREE.Vector3` + `camera.position.clone()` per frame while the HUD card is anchored.
- **S3** 🟠 (F) **Wall**: 2 MB JS (630 KB gz), reflector (second full scene render at 512² + blur) + Bloom mipmap + SMAA + two `RectAreaLight` on a 400 m plane at DPR 1.5 with no mobile tier — only `SOFTWARE` downgrades. Long tasks 1445 + 553 ms on iPhone. `wall/components/WallCanvas.tsx:98-106`, `WallScene.tsx:193-206`. `App.tsx:339-395` `started`/`card`/`tour` are App state and `WallCanvas` is not memoised: every tour step re-renders `<Canvas>` and `WallScene`.
- **S4** 🟠 (D) **Darkroom**: 20 Jacobi iterations + ~30 passes/frame + composite at DPR 1.5 with two `develop()` and three noises; the only downgrade is software/no-half-float. 332 ms long task on mobile. `darkroom/components/darkroom-engine.ts:929-934,1234-1240`. `:184,597-627` `TextureLoader` (main-thread `<img>` decode), no `ImageBitmapLoader`.
- **S5** 🟠 (D) **Mani**: wasm (11.8 MB) + model (7.8 MB) downloaded only after the grant and inside the 8 s `INIT_TIMEOUT_MS`: on a cold mobile network the timeout fires ("the camera isn't responding") and retry re-runs the same race. `FilesetResolver.forVisionTasks` needs no permission and could start at gate mount. `hands/hooks/useHandTracker.ts:126-164`. `useHandTracker.ts:295` `new Float32Array(42)` per hand per detection; `HandsScene.tsx:223,242-246` `getCurrentViewport()` + three `.map` per frame; `world.ts:405-415` `holders` array per frame.
- **S6** 🟠 (C) `public/mediapipe/wasm/vision_wasm_module_internal.{js,wasm}` (12 MB) never requested (`forVisionTasks` picks `vision_wasm_internal` or `_nosimd_internal`).
- **S7** 🟠 (C) **/graphic-designs covers blurry on phones**: `sizes="(max-width:720px) 40vw, 220px"` but under 560 px the cover fills the card (~320 px at 390): served at 156 px for a 309 css px / 927 device px box; between 561–720 px it over-requests (cover is 28vw). `src/app/(site)/graphic-designs/page.tsx:92` vs `globals.css:2600-2604`. Vortex cover is `img_001.webp` 640×609 cropped by `objectFit: cover` (`page.tsx:30,100`) instead of an 800×1000 capture; below-the-fold covers all `loading="eager"`.
- **S8** 🟠 (B) **/websites**: images served at 1120 px for 1181 css px desktop and 358 px for 1077 device px mobile; 1120×700 source cropped into a 3.5:1 box; `loading="lazy"` although above the fold.
- **S9** 🟠 (B) /about: LCP image with `loading="auto"` and no `fetchpriority="high"`; long task 166 ms desktop / 307 ms mobile; CLS 0.05. `FigureView.tsx:158,182` 2 `getBoundingClientRect` per frame per figure + 1 from the View → ~7 forced layout reads/frame.
- **S10** 🟠 (B) NameMelt's 20 fps idle floor invalidates the demand loop and drei `View` redraws every view (`node_modules/@react-three/drei/web/View.js:97-114`): the fullscreen ambient field renders at ≥20 fps even under `staticOnly` (software renderer) — the "one static frame" guard is defeated on the home because `NameMelt` does not know `staticOnly` (local state in `Field.tsx:31`). `NameMelt.tsx:23`, `NameMeltView.tsx:207-216`.
- **S11** 🟠 (B) `Row.tsx:82-129` mounts N×2 close tweens + a `lenis.resize()` per row on /websites and /xperiments. `Field.tsx:68` `dpr={[1,1.5]}` read only at configure: moving to another monitor keeps the old DPR (`NameMeltView.tsx:351-361` re-rasterises correctly).
- **S12** 🟠 (E) Tarassaco: DOM physics writing `style.transform/opacity` on ~140 SVG `<g>` plus every word per frame; SVG children get no compositor layer (`willChange` inert), whole 450 px SVG repaints every frame while blowing. `useWindPhysics.ts:458-528`, `GlowingDandelion.tsx:101-127`.
- **S13** 🟠 (A) ~160 lines of Vortex CSS live in `globals.css:2361-2519`, served on every site page (the other four demos have their own file).
- **S14** 🟠 (C) `opengraph-image.tsx:9-19` downloads CSS + TTF from Google Fonts at build time, regex on the response, `throw` on mismatch: build fails offline or on a format change; no local fallback.
- **S15** 🟠 (C) `/mediapipe/:path*` and `/vortex/images/:path*` immutable with files already re-encoded in place in git (`d39c3d6` wasm 9.5→11.7 MB; `ca3ac2b` 18 vortex webp re-encoded): returning visitors keep the heavy versions.

## I — Inconsistencies

### Copy and naming (C owns the dictionary; demo `copy.ts` belong to D/E/F)

- **I1** 🟡 (C) "Merge" in three forms: `home.teasers.graphic` "Merge Graphic Designs" (:226/:438) · `gd.title`/`gd.metaTitle` "Merge — Graphic Designs" (:370/:375) · vortex `metaTitle` "Image Vortex — Merge" (`vortex/copy.ts:56/:72`, only demo with the suffix → **E**). "graphic designs" (`nav.graphic` :208/:420) vs "graphic design" (`work.sections.graphic` :234/:446, `gd.label` :374/:586). IT nav "siti" (:419) vs h1 "Siti web" (:563) vs `work.sections.websites` "siti web".
- **I2** 🟡 (C) IT untranslated: `nav.about` "about" (:416), `about.metaTitle`/`about.title`/`about.label` "About" (:491-495) → IT `<title>` "About — Alberto Marocco.dev"; `nav.graphic` "graphic designs" (:420), `gd.label` (:586). If brand words, document it in DECISIONS.
- **I3** 🟡 (E) Exit link: vortex `exit: "✕ exit the demo"` (`vortex/copy.ts:61/:77`) vs "← exit the demo" in the other four; tarassaco "← ESCI DALLA DEMO" uppercase with its own style (`.tara-exit` at `top:2rem;left:2rem`, white inversion). Gate register: tarassaco "CLICK TO ENABLE"/"INITIALIZING"/"INIZIALIZZO" (`tarassaco/copy.ts:49-50/:73-74`) vs hands "enable the camera"/"initializing"/"avvio in corso"; "INIZIALIZZO" reads as first person.
- **I4** 🟡 (F, C) "LED wall" vs "led wall" inside EN: dictionary :199, :218, :400-402 uppercase; `wall/copy.ts:73-74` "LED wall" but `:80` `spec.wall` "led wall", `:93` `tour.label` "what is a led wall? →", `:94` `tour.aria` lowercase; ⚪ "a LED wall" → "an LED wall" (:402, wall/copy.ts:73/74/93/94). "6×3m" (`work.items.liminal-field.description` :261/:473) vs "6 × 3 m" (`gd.demos.wall.desc` :402/:614, `wall/copy.ts:73/:156`).
- **I5** 🟡 (C, E, D) Apostrophes: dictionary all straight `'` except `it.gd.demos.wall.desc` :614 ("com’è" curly); `vortex/copy.ts:65` "doesn’t" curly vs `hands/copy.ts:111-113,125` and `darkroom/copy.ts:48` straight; `(immersive)/graphic-designs/error.tsx:21` EN curly vs `:25` IT straight.
- **I6** 🟡 (F) Wall `metaDescription` promises "switch loops"/"cambia loop" (`wall/copy.ts:73/:156`) but the control changes the palette of one loop ("Liminal Field" `:87`; `loopAria` "loop palette" `:90`, `keyboardHint` "← → palette" `:78`, `gd.demos.wall.desc` "switch palettes" :402).
- **I7** 🟡 (C) Titles with three segments and the same separator: "Camera Oscura — Darkroom — Alberto Marocco.dev" (46 chars), "Merge — Graphic Designs — Alberto Marocco.dev" (45), "Tarassaco — Dandelion Wind — Alberto Marocco.dev" (48). Template `%s — Alberto Marocco.dev` `layout.tsx:42`. Descriptions over ~160 chars: tarassaco EN 178 / IT 196 (`tarassaco/copy.ts:47/:71` → **E**), hands EN 181 / IT 191 (`hands/copy.ts:87/:133` → **D**), wall EN 213 / IT 221 (`wall/copy.ts:73/:156` → **F**).
- **I8** 🟡 (C) Brand in three spellings: "Alberto Marocco.dev" (`layout.tsx:41,42,45,65,66,71`) · "albertomarocco.dev" (`opengraph-image.tsx:24`, `Footer.tsx:50`, `README.md:1`) · alt OG "Alberto Marocco — Creative Technologist" (`opengraph-image.tsx:3`). Domain hardcoded in `layout.tsx:27`, `sitemap.ts:3`, `robots.ts:6-7`.
- **I9** 🟡 (C) Dead dictionary keys (zero references, the `Dictionary` interface forces them in both languages): `nav.work` (:204/:416), `nav.contact` (:205/:417), `gd.back` (:373/:585), `about.label` (:279/:491), `about.title` (:283/:495), `work.aria` (:231/:443), `work.sections` (:232-236/:444-448), `work.items["merge-graphic-designs"]` (:252-257/:464-469), `error` (:368/:580 — `error.tsx` reads `ERROR_COPY` directly).
- **I10** ⚪ (C) `about.panels.contact.labels.where` EN "based in" (:340) vs IT "dove" (:552); `note` "en / it" (:343) vs "it / en" (:555); `en.meta.description` (:199) and `en.about.metaDescription` (:282) both open with "Creative technologist … in Turin" (IT :411/:494). `darkroom/copy.ts:56` aria IT "Camera Oscura — camera oscura interattiva" (tautology → **D**). `vortex/copy.ts:79` IT "← vortice" translates a name that stays "Image Vortex" elsewhere (→ **E**). `vortex/copy.ts:1` and `tarassaco/copy.ts:1` `import type { Locale } from "@/lib/dictionary"` vs `@/lib/locale` in the other three (type-only, harmless → **E**).
- **I11** ⚪ Demo subtitles in English in both languages: `darkroom/components/Hud.tsx:31` "darkroom", `hands/components/Gate.tsx:24` "hands", `tarassaco/App.tsx:153` "dandelion wind", `wall/components/Hud.tsx:66` "led wall" — by convention; `src/app/(site)/about/page.tsx:229-235` pager buttons' accessible name is "01/02/03" only (→ **B**); `src/app/layout.tsx:93-96` JSON-LD `addressLocality: "Turin"`, `jobTitle` EN regardless of locale, `email: "mailto:…"` (schema.org expects the bare address) (→ **C**).
- **I12** ⚪ (C) `(immersive)/graphic-designs/loading.tsx:36` and the two `error.tsx` render "caricamento"/"errore" at SSR and hydration, then swap to "loading" for EN users: flash in the wrong language. `LocaleToggle.tsx:10` cookie without `Secure`. No `Accept-Language` negotiation (documented choice). No `hreflang`/`alternates.languages`: crawlers see only IT, all EN descriptions invisible to search engines and social previews; `og:locale` for bots always `it_IT` (structural limit of the cookie-based locale — document it).

### Accessibility and touch

- **I13** 🟡 (A: globals.css; D/E/F: demo css) Text under 12 px: topbar 0.6rem (9.6 px) under 560 px and 0.55rem (8.8 px) under 380 px (`globals.css:447-455,469-473`); mono register 0.62–0.72rem in eyebrow :517, teaser-idx/cue :860,913, about-cue/pager :1648,1694, contact-key :1975, footer :2031; demo HUDs 9.3–10.9 px (`wall-title-sub`, `wall-spec`, `hands-privacy`, `hands-legend-seg`, `darkroom-hint-keys`).
- **I14** 🟡 (A; D/E/F) Tap targets < 44 px: locale toggle 14×13 px, nav 19 px tall, footer links 15 px (`globals.css:2026-2047`), `.about-cta` ~14 px (:1555-1565), `.about-pager button` ~21 px (:1688-1703), topbar hit-area ~38 px nav and ~26×35 px en/it (:488-504); exit link ~28-32 px in all five demos (font 0.7rem + padding 0.5rem); "or use the pointer" ~20 px and "try the camera again" (`hands.css:299-309,452-466`); `.darkroom-next` ~28 px. Only the wall has a `pointer: coarse` rule.
- **I15** 🟡 (A) `outline:none` on `:focus-visible` with only a colour change `--ink-dim → --ink` (≈2.2:1 between states, < 3:1 required): weak focus on topbar, toggle, CTAs, pager, contacts, footer (`globals.css:387-391,436-440,1570-1574,1715-1719,1992-1997,2048-2052`). (D) `.hands-stage { outline: none }` on a focusable `role=application` (`hands.css:22-34`).
- **I16** 🟡 (D/F) `touch-action: none` on the whole stage in darkroom/hands/wall (`darkroom.css:25`, `hands.css:29`, `wall.css:25`) → pinch-zoom disabled on the whole page (WCAG 1.4.4); vortex and tarassaco allow it (vortex `.vortex-frame` has no `touch-action` at all → gesture leaks → **E**).
- **I17** 🟡 (D/E/F) Stage semantics differ per demo: darkroom `role=region`; hands `role=application tabIndex=0`; wall `role=region tabIndex=-1`; vortex `role=application`; tarassaco nothing — no `aria-label`, no `noWebgl`, no `contextLost` handling (`tarassaco/copy.ts:15-42`), stage stays black without WebGL. No-WebGL/context-lost messages have `role="alert"` in darkroom and wall, bare div in hands and vortex.
- **I18** 🟡 (D/E) Key guards: darkroom, wall, tarassaco filter repeat + modifiers; hands and vortex do not (B7, B8). (B) `e.target` guard on keys only in `AboutSequence.tsx:239-241`: on the home Space/arrows with focus on topbar buttons scrub the sequence (`HomeSequence.tsx:511-547`).
- **I19** 🟡 (B) Duplicated logic that can drift: wheel/touch normalisation (`deltaMode`, `LINE_PX`) in `HomeSequence.tsx:102,490-509` and `AboutSequence.tsx:62,186-233`; touch detection `(hover:none)` in `Cursor.tsx:18` and `WorkRows.tsx:38` vs `(pointer:coarse)` in `HomeSequence.tsx:139`. (D/F) `.darkroom-exit`/`.hands-exit`/`.wall-exit` are literal copies of `.vortex-exit` in three files (`darkroom.css:44-79`, `hands.css:161-185`, `wall.css:43-67`).
- **I20** 🟡 (D/E) Gate/permission-denied pattern: hands = camera + pointer alternative + privacy + dialog with retry; tarassaco = one button, dialog without retry, no keyboard-first entry (`tarassaco/components/GateScene.tsx:15-34`). Reduced motion: darkroom (brush), wall (still), hands (partial), vortex (cut) have their own path; tarassaco relies only on the global `* { animation-duration: .001ms }` (`globals.css:2331`).
- **I21** ⚪ (B) `src/components/work/Row.tsx:225-236` `<button>` containing `<div>`s (row-head, row-reveal, View div): non-phrasing content in a button; accessible name = index+title+meta+description+cue. `src/lib/use-reduced-motion.ts:8` comment points to a private memory note.
- **I22** ⚪ Tarassaco style drift: font `"Inter"` never loaded and `ui-monospace` stack instead of `var(--mono)` (`tarassaco.css:25,112,203,226`); title bottom-centre vs top; inline styles in `GateScene.tsx:38-53`; only demo with `lucide-react` (3 icons); `getUserMedia` fires in an effect after a `setState`, not from the click (`App.tsx:122`). Vortex: font `'Inter'` in `VortexScene.jsx:52`, `GalleryScene.jsx:229`. `loading.tsx:24` and `error.tsx:45` use `ui-monospace` inline instead of `var(--mono)`. `dynamic()` chunk loading shows a black div in every demo (no `loading` option); only vortex has a texture-progress overlay.

### Dead code, docs, config

- **I23** 🟡 (B) `src/lib/work.ts:102` `WORK` exported and never imported; `mediaGradient` (:33,55,65) and the plate branch in `Row.tsx:185-191` dead (both web rows have `image`); `globals.css:318-320` `.wrap.in{opacity:1}` without a base at 0 and `Shell.tsx:187` toggling `in` on `.wrap` (dead rule → **A**); `AboutSequence.tsx:63` `#contatti` alias nothing produces.
- **I24** 🟡 (C) `DECISIONS.md` drift: "Next 16.2.9" (:9) vs 16.3.4; "`@react-three/postprocessing` deliberately omitted" (:13-15) vs used by `wall/components/WallCanvas.tsx` and `vortex/components/VortexScene.jsx`; BLOB_COUNT 7 vs 14 (`field-glsl.ts:20`); CORE_RADIUS/IDLE_DRIFT/BASE_SPEED 0.15/0.13/0.5 vs 0.055/0.065/0.25 (`Aura.tsx:31-37`); `maxFade` 0.45 vs 0.7 (`AmbientField.tsx:39`); "field gated on hero IntersectionObserver" vs always active (`Field.tsx:23-28`); gate + click entrance + `enter()` (:33-44,104,166-176) removed; P.IVA `00000000000` (:290); instagram "guessed handle" (:291) vs `contact.ts:12-14`; "Studio — next … `#work`" (:292-297) vs `work.ts:90-96`; "visit site ↗ label owned by work.ts" (:311-312) vs dictionary. `README.md:27` lists a `gate` chrome component that does not exist; tree (:22-48) misses `components/home/`, `components/vortex/`, `canvas/NameMelt*`, `melt-material`, `BubbleControls/BubblePanel`, `lib/webgl-caps`, `use-reduced-motion`, `use-tab-visible`; "layout (fonts, metadata, providers)" but providers live in `(site)/layout.tsx`; `:62-64` "real media for the web rows" stale; `:10-12` stack omits postprocessing, MediaPipe, lucide, JetBrains Mono. `reference/briefs/00-shared-rules.md` and round-1 briefs/reports: 35 references to `/xperiments/<id>`.
- **I25** 🟡 (C) `sitemap.ts:6` `lastModified = new Date()` at build: every deploy marks all 10 URLs as modified now. No `manifest`, no `apple-icon` (only `icon.svg`). `layout.tsx:53` keyword "webgpu" while `DECISIONS.md:606` says WebGPU is deliberately not attempted.
- **I26** ⚪ (C) `next.config.ts:19-36` Permissions-Policy `camera=(self)`/`microphone=(self)` is a no-op (spec default allowlist is already `self` for a top-level document); the comment is wrong. No CSP, no `X-Content-Type-Options`/`Referrer-Policy`. `images` without config: AVIF not enabled.
- **I27** 🟡 (C) `toretto-raw.png` (254 KB) tracked at the repo root, referenced nowhere. `eslint.config.mjs:19` + `tsconfig.json:33` + `.gitignore:51` all carry an exception for `src/Merge Designs/` (64 MB untracked archive living inside `src/`).
- **I28** ⚪ (C) `npm ls`: `@emnapi/runtime@1.11.3` and `@img/sharp-wasm32@0.35.4` extraneous (local install drift; `npm ci` removes them). `npm outdated`: patches `next` 16.3.5, `eslint-config-next` 16.3.5, lucide 1.45.0; majors blocked (react 19.3, TS 7, eslint 10, @types/node 26).

## Verified and fine

EN/IT parity complete (dictionary 126 leaves, vortex 25, tarassaco 18, darkroom 12, hands 51, wall 58); IT tone "tu" throughout; no client component imports `@/lib/dictionary` at runtime; `ssr:false` only in client components; `viewport` as a separate export; canonical on every route; sitemap = the 10 real routes; 308 redirects from `/xperiments/<id>` and trailing slashes; Escape exits all five demos; camera/mic/landmarker/RAF/AudioContext closed on unmount; no `document.body` style leaks; no duplicate canvas and no leaked rAF loop after 4 home ↔ about round trips (rAF steady at 20/s); no hydration mismatch sources; lockfile aligned; `.gitignore`/`tsconfig` for `next-env.d.ts` per Next 16 docs; locale switch works and persists; console clean on all 10 routes desktop + mobile.

## Measurements (production build, local)

| route | JS raw / gz | LCP | notes |
|---|---|---|---|
| / | 1499 KB / 432 KB | 224 ms | fonts 185 KB (3 woff2), CLS 0 |
| /about | 1517 / 434 | 288 ms (img) | CLS 0.05, long task 166 ms (307 ms mobile) |
| /graphic-designs | 1506 / 431 | 220 ms | 5 covers all eager |
| /websites | 1500 / 427 | 192 ms | 2 imgs lazy above the fold |
| /xperiments | 1500 / 427 | 156 ms | |
| vortex | 1700 / 515 | 332 ms | + 54 imgs 1.7 MB, long task 647 ms |
| tarassaco | 753 / 237 | 344 ms | no three at gate |
| darkroom | 1525 / 433 | 328 ms | long task 86 ms (332 mobile) |
| hands | 1680 / 481 | 356 ms | + 14 imgs 479 KB, long tasks 52/62/137 |
| wall | 2004 / 630 | 340 ms | long task 144 ms (1445 + 553 mobile) |
